prog basic_soma
;

tipo arr1 := vetor[4 .. 10] de texto;

var a : arr1;
var b : vetor[4 .. 10] de texto;

** STRUCT DECLARATION
tipo TDU := registro 
        var nome : texto; 
        var idade : inteiro
    fim;

var c : TDU;

var bla : inteiro; 
var ble : inteiro;

tipo range1 := bla .. c.idade;

** ENUM DECLARATION
tipo enum := (jan, fev, mar);

inicio
fim
